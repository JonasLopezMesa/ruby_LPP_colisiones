Modificamos el mismo fichero (matrix2.rb) en dos ramas distintas (master y sergio). Al hacer un merge, esto generó un conflicto,
para el cual se llegó a un consenso y se eliminó uno de los shebangs y se dejó el resto de funciones de manera ordenada.
